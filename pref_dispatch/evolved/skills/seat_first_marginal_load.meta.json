{
  "skill_name": "seat_first_marginal_load",
  "objective": "Maximise the number of distinct riders served this hour by grabbing every feasible order when a seat is free, sacrificing per-fare revenue and detour efficiency to stop riders going unmatched.",
  "description": "Any order that physically fits the free seats is worth a fixed, saturating value — one served rider is one served rider, so there is no fare or length preference. What separates candidates is only the burden the insertion puts on riders already committed: an empty car has nobody to delay, so its burden is zero and it grabs the first feasible order in the step; a vehicle already carrying passengers pays a burden equal to the pickup (and ride detour) weighted by its load, so in a slack market a near-full car only inserts very close pickups and otherwise prefers to wait. Demand pressure is the relaxer in both directions: when riders are piling up unmatched, the burden and the waiting floor both collapse, so even a loaded car takes every feasible seat-filler rather than hold out. The noop floor is deliberately tiny when a car is empty — waiting costs a served rider — and it grows with load (protecting banked service during a slack hour) but shrinks under pressure, so the mechanism free-runs between 'grab everything' and 'protect what's on board' purely off the scene's own load, time and pressure scales.",
  "fitness_code": "def fitness(metrics):\n    return 0.55 * metrics['service_rate'] + 0.45 * (metrics['assigned'] / 8400.0)",
  "fitness_rationale": "Service_rate is the direct measure of the objective — the fraction of riders served rather than matched/revenue — and the assigned-volume term in mean_solo-time-scaled units rewards the raw number of riders moved, so a skill that grabs every feasible order scores high on both.",
  "code": "def score(driver_obs, order, phi_ep, phi_step):\n    self = driver_obs['self']\n    cap = self['capacity']\n    party = order['num_passengers']\n    committed = self['committed_passengers']\n    free = cap - committed\n    if free <= 0 or party > free:\n        return -1e9\n\n    scale = phi_step.mean_solo_time\n    if scale is None or scale <= 0:\n        scale = phi_ep.scale\n    if scale is None or scale <= 0:\n        scale = 1.0\n\n    pickup = phi_ep.dist(self['location'], order['origin']) / scale\n    ride = phi_ep.dist(order['origin'], order['destination']) / scale\n\n    # Saturation: any feasible order is a fully served rider --- value plateaus.\n    base = 1.0\n\n    # Marginal / counterfactual: burden on riders ALREADY committed.\n    load = float(committed) / float(cap)\n    onboard = 0\n    for od in self['assigned_order_details']:\n        if od.get('onboard', False):\n            onboard += 1\n\n    # Demand pressure relaxes the burden: unmatched riders make even a loaded\n    # car take a feasible seat-filler instead of holding out.\n    pressure = phi_step.demand_pressure\n    relax = 1.0 / (1.0 + pressure)\n\n    burden = 2.5 * load * pickup * relax\n    if onboard > 0:\n        burden += 0.6 * load * ride * relax\n\n    return base - burden\n\n\ndef noop_score(driver_obs, phi_ep, phi_step):\n    self = driver_obs['self']\n    cap = self['capacity']\n    committed = self['committed_passengers']\n    free = cap - committed\n    scale = phi_step.mean_solo_time\n    if scale is None or scale <= 0:\n        scale = phi_ep.scale\n    if scale is None or scale <= 0:\n        scale = 1.0\n\n    if free <= 0:\n        return 1.5\n\n    load = float(committed) / float(cap)\n    pressure = phi_step.demand_pressure\n    # Empty cars have nothing to protect -> grab anything (floor ~0).\n    # Loaded cars' floor rises with load -> wait unless a cheap insert appears.\n    floor = 0.02 + 0.7 * load\n    # Unmatched riders destroy the luxury of patience -> floor collapses.\n    floor = floor / (1.0 + 1.5 * pressure)\n    return max(0.0, floor)",
  "gen": 3,
  "mechanism": "marginal/counterfactual cost: a saturating base value for 'one more rider served' minus the marginal detour burden this insertion places on the riders already committed to the vehicle, ranked against an inverted-patience noop floor",
  "differs_from": "Closest to near_short_efficiency in that it never rejects on trip length and grabs almost everything, but it scores on a completely different axis: not the order's own attributes (fare ratio like gowin_ratio_gate, or own efficiency like near_short_efficiency) but the counterfactual CHANGE the insertion causes — the marginal delay it inflicts on the vehicle's existing commitments. An empty car pays literally zero marginal burden, so it takes the first feasible order greedily; a loaded car's burden scales with its load, so it only inserts very short pickups that don't endanger riders it already has.",
  "operator": "fresh(parentless)",
  "parents": [],
  "audit": {
    "status": "match",
    "verdict": "match",
    "reason": "The table is a coherent, recognisable instance of 'grab every feasible rider first, tolerate burden as a tiebreaker.' Assigned volume tracks the objective: scenarios with slack capacity (f581, f1002, f1114) reach service rates of 0.67–0.98 while pushing 6900–10213 assigned riders, and the low-revenue/low-cost f581 row shows the intended sacrifice — worst detour/order (8.37) and negative income min, yet the highest assigned count (10213) and best fitness (+0.92). The loaded-car patience behaviour is visible too: f208 with capacity 10 and the highest scheduler cost (s30.95) records only 0.394 service and 3250 assigned, i.e. it held out rather than fill seats, while near-capacity-low-cost runs (f560, f1114) saturate. Nothing in the table contradicts the stated mechanism; the revenue sacrifice the objective accepted is present, and the one apparent miss (f208's low grab-rate) is exactly the 'protect what's on board' branch the description forecasts for loaded cars in a slack market.",
    "evidence": "f581 (assigned 10213, serv_rate 0.674, detour 8.37, fitness +0.92) versus f208 (assigned 3250, serv_rate 0.394) and f1114 (serv_rate 0.972) map directly onto the grab-everything vs protect-loaded-car branches of the intended mechanism.",
    "fitness_complaint": "",
    "attempt": 1,
    "searches": 1,
    "behaviour_table": "scenario                  |  fitness | assigned | serv_rate | completed | svc_min | detour/order | revenue | gini | income_min\n------------------------------------------------------------------------------------------------------------------------------\nf581_c10_s39.2_Fri19h_rev0.033 |    +0.92 |    10213 |     0.674 |      7430 |    9.48 |         8.37 |   90914 | 0.16 |     -11.49\nf1114_c3_s32.27_Fri14h_rev0.638 |    +0.93 |     7431 |     0.972 |      5876 |   11.14 |         2.31 |   94086 | 0.19 |       0.10\nf208_c10_s30.95_Sat10h_rev0.705 |    +0.39 |     3250 |     0.394 |      2099 |   12.96 |        11.42 |   32516 | 0.21 |     -13.68\nf560_c5_s40.43_Fri09h_rev0.411 |    +0.77 |     6221 |     0.798 |      5069 |    8.69 |         3.77 |   65297 | 0.18 |       0.08\nf1002_c9_s38.31_Wed12h_rev0.955 |    +0.91 |     6905 |     0.983 |      5287 |    9.99 |         5.39 |   88798 | 0.27 |      -3.77\nf364_c7_s36.62_Wed09h_rev0.255 |    +0.59 |     5258 |     0.562 |      3933 |    9.79 |         6.93 |   47665 | 0.18 |      -6.17\n------------------------------------------------------------------------------------------------------------------------------"
  },
  "regime": "scenarios",
  "eval_mode": "group",
  "fitness": 0.7333975141462038,
  "raw_fitness": 0.7333975141462038,
  "per_scenario_adv": [
    1.6277438541707996,
    -0.14607894242408676,
    1.5210273260216203,
    -0.11819458682790814,
    0.3779644730082729,
    1.1379229609285244
  ],
  "per_scenario_raw": [
    0.9176738488686589,
    0.9326236827846495,
    0.390826347512817,
    0.7720968939518863,
    0.9108236896453497,
    0.5905762428662984
  ],
  "per_band": {
    "fleet500-1000": 0.7547746336714457,
    "fleet1000-1500": 0.11594276529209308,
    "fleet200-500": 1.3294751434750722
  },
  "invalid_rate": 0.0,
  "scenario_labels": [
    "f581_c10_s39.2_Fri19h_rev0.033",
    "f1114_c3_s32.27_Fri14h_rev0.638",
    "f208_c10_s30.95_Sat10h_rev0.705",
    "f560_c5_s40.43_Fri09h_rev0.411",
    "f1002_c9_s38.31_Wed12h_rev0.955",
    "f364_c7_s36.62_Wed09h_rev0.255"
  ]
}